{
  "runtime": "node",
  "node_version": "22",
  "memory_max_mb": 768,
  "build_command": "corepack enable && corepack prepare pnpm@9.15.3 --activate && HUSKY=0 pnpm install --frozen-lockfile && pnpm run build",
  "start_command": "npx next start --hostname 127.0.0.1 --port ${PORT}"
}
